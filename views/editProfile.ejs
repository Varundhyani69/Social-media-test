<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gallery kinda somethin...</title>
    <link rel="stylesheet" href="/css/changeBio.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
</head>
<body>
    <div class="submit-container shadow">
        
            <% if (success && success.length > 0) { %>
                <% success.forEach((msg) => { %>
                    <div class="flash-success"><%= msg %></div>
                <% }) %>
            <% } %>
        
            <% if (errors && errors.length > 0) { %>
                <% errors.forEach((msg) => { %>
                    <div class="flash-error"><%= msg %></div>
                <% }) %>
            <% } %>
        
        <div class="textChange">
            
            <form action="/api/auth/editProfile" enctype="multipart/form-data" method="post">
                <div class="pfpBox mb-3 ">
                    <div class="curr">
                        <img  class="pfp shadow" src="<%=user.pfp.url%>">
                    </div>
                    <div class="input-group inp mb-2 mt-2">
                        <input type="file" class="form-control" name="pfp" id="inputGroupFile02">
                    </div>  
                </div>
                <div class="mb3 log-text"><label for="Enter Bio"><h3><b>Change BIO</b></h3></label></div>
                <div class="mb3 text-box">
                    <textarea name="newBio" class="text-area" placeholder="<%=user.bio===''?'Enter bio':user.bio%>"><%=user.bio%></textarea>
                </div>
                <div class="mb3 btn-box">
                    <button class="btn submit-btn btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
        
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
</body>
</html>